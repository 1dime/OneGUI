Use CMake for compilation
    - Use linker to link all c++ code
    
Create a linked list class
    - Holds node as head
    - Can add nodes
    - Can insert nodes at position
    - Can remove nodes

Create a widget class
    - Must have widget data
    - Must have redefinable draw and widgetFunction functions
    - Must be a linked list